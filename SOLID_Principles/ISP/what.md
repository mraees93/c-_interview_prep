An interface should be very closely linked to the class that uses it rather than the code that implements it.

e.g

violating the ISP would be having one big bulky interface with properties and methods you are not going to use