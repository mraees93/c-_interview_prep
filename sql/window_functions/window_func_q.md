-- Schema Details:
-- Stores (StoreID, StoreName, City)
-- Employees (EmployeeID, StoreID, FullName, Role, HireDate)
-- SalesBooks (BookID, Title, Category, BasePrice)
-- Transactions (TransactionID, StoreID, EmployeeID, BookID, SalePrice, TransactionDate)

-- Question 1-- Select the TransactionID, EmployeeID, SalePrice, and TransactionDate, alongside a column that assigns a sequential row number to every transaction across the entire database, ordered from the newest transaction date to the oldest.

-- Question 2-- Display the EmployeeID, FullName, Role, and HireDate, along with a column that assigns a rank to employees within each specific Role based on their seniority (earliest HireDate gets rank 1). If there is a tie, they must share the same rank number, and the next rank number in the sequence must skip ahead.

-- Question 3-- Find the TransactionID, StoreID, SalePrice, and TransactionDate, along with a column showing the highest individual SalePrice recorded within that specific transaction's StoreID up until that point in time (ordered chronologically by TransactionDate).

-- Question 4-- Select the EmployeeID, StoreID, Role, and HireDate, along with a dense ranking that orders employees inside each StoreID by their length of service (longest-serving employee gets rank 1), regardless of their Role.

-- Question 5-- Display the BookID, Title, Category, and BasePrice, alongside a column showing the average BasePrice of all books belonging to that specific book's Category. Do not use a GROUP BY clause.

-- Question 6-- Write a query to show every TransactionID, StoreID, BookID, SalePrice, and a column calculating the exact difference between the transaction's individual SalePrice and the overall average SalePrice of that specific StoreID.

-- Question 7-- Generate a output showing the TransactionID, EmployeeID, SalePrice, and TransactionDate, along with a column that divides all transactions across the entire database into exactly 4 equal horizontal groups (quartiles) based on the SalePrice from lowest to highest.

-- Question 8-- Select the TransactionID, StoreID, SalePrice, and TransactionDate, along with a column that displays the moving average SalePrice for that specific StoreID. The moving average must only calculate the current transaction and the immediate 2 transactions that occurred directly before it chronologically.

-- Question 9-- Find the TransactionID, BookID, SalePrice, and TransactionDate, alongside a column calculating the exact percentage contribution (%) that each individual sale's price represents out of the total combined revenue ever generated by that specific BookID.

-- Question 10-- Write a query to extract the complete information (TransactionID, StoreID, EmployeeID, BookID, SalePrice, TransactionDate) for only the transactions that represent the single absolute lowest SalePrice recorded for each individual BookID. If a BookID has a tie for its lowest sale price, return all matching tied rows.