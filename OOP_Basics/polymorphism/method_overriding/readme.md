- When a child class provides a new implementation of a method that is already present in the parent class, it is called method overriding.

- Binding Type: Late Binding (Dynamic Binding) → The method to be called is determined at runtime.

Requirements:
- The parent method must be declared as virtual.
- The child method must be declared as override.